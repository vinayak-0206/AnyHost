run = "go build -o gotunnel-unified ./cmd/unified && ./gotunnel-unified --log-level info"
entrypoint = "cmd/unified/main.go"
modules = ["go-1.21"]

[nix]
channel = "stable-24_05"

[env]
DOMAIN = "gotunnel.repl.co"

[deployment]
run = ["sh", "-c", "go build -o gotunnel-unified ./cmd/unified && ./gotunnel-unified --log-level info"]
deploymentTarget = "cloudrun"
